import { MarkUpColorDirective } from './mark-upred.directive';

describe('MarkUpredDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkUpColorDirective();
    expect(directive).toBeTruthy();
  });
});
